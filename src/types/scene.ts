/**
 * Shared across siteConfig (schedule data) and the scene-related components,
 * so config doesn't need to import from a component module.
 */
export type SceneMode = 'default' | 'rain' | 'tapri'
